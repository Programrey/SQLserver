# SQLserver
pura documentacion de mis primeros pasos con SQLserver en README
al instalar la aplicacion le dia a la opcion personalizada le dia  los componentes que necesitaba no lo vincule con azeure
luego me dio predeterminado los datos de mi cuenta 
Service: SQL Full-text Filter Deamon Laucher
Account Name: NT service/MSSQLFDLaucher$SQLEXPRESS 
Password: 
Startup Type: Manual


avansando 
tengo un proyecto de en java, quiero hacer un login y estoy usando este scrip 

CREATE TABLE usuarios (
    id INT IDENTITY(1,1) PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);
 intente hacerlo tambien manualmente.
